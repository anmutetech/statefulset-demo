# In production, use an external secret manager (AWS Secrets Manager, Vault, etc.)
# This is for lab purposes only
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  POSTGRES_PASSWORD: "labpassword123"
  REPLICATION_PASSWORD: "replpass"
